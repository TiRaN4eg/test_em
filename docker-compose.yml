services:
  nginx:
    image: nginx
    container_name: custom_nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: always
  backend:
    build: ./backend
    container_name: custom_backend
    restart: always
